
Describe a clear step-by-step diagram for [OPERATION, e.g. "deleting a node from a singly linked list"]. For each step:
- State what the structure looks like before the operation
- Show pointer changes using arrow notation, e.g. A -> B -> C becomes A -> C
- Label any temporary pointers (LOC, LOCP, TEMP etc.)
- End with the final state of the structure
Format each step as: Step N: [label] followed by the arrow diagram on the next line.
Keep it text-based so it can go directly into a markdown note.